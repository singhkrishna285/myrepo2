#include<stdio.h>
#include<string.h>

int main()
{
	char str[50]="krishna";
	char str1[50]="singh";
	printf("original string is %s\n",str);
	printf("Reversed string is %s\n",strrev(str));
	printf("concatenation of two string is %s\n,strcat(str,str1));
}
